Working in a direcotory:
C:\Users\kardoost\Desktop\NODEjs\website_Database_Connected\JoinUsCompleteCode\JoinUsMine

The order of the code I ran:

1. npm init

2. entry point: (index.js) app.js

3. npm install express --save

4. npm install faker mysql --save

5. If I only give "package.json" to someone, and they run "npm install"
then it will automatically install all the required modules like faker and mysql and ...

6. Created empty file "app.js" file and insert the code inside

7. run "node app.js"

8. open browser and hit "localhost:8080"

-------------------
9. npm install ejs --save

10. create folder "views" inside that directory and create a file named "home.ejs" inside the "views" directory

11. write lines into "home.ejs" file and on "app.js" file

// the next line helps to parse the request body
12. npm install body-parser --save 

13. npm install xml2js --save


